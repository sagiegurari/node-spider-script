
func Person(name) {
  this.name = name;

  this.listen = func (text) {
    console.log(this.name, text);
    return this.name + ' ' + text;
  };
}

return {
    create: func(name) {
        return new Person(name);
    }
};
