
fn Person(name) {
  this.name = name;

  this.listen = fn (text) {
    console.log(this.name, text);
    return this.name + ' ' + text;
  };
}

return {
    create: fn(name) {
        return new Person(name);
    }
};
